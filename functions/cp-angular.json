const fs = require('fs-extra');

const fs = require('fs-extra');

(async() => {

const src = path.resolve('../dist');
const copy = path.resolve('./dist');

await fs.remove(copy);
await fs.copy(src, copy);

},
err =() =>{
console.log('Please ensure that a dist folder has been created in the root directory as well as the functions directory')
})();

